@inherits LayoutComponentBase
@inject IJSRuntime JS
@inject ILocalStorageService localStorageService

<main data-bs-theme="@ThemeName">
    <header>
        <nav class="navbar navbar-expand-lg @( ThemeName == _darkThemeName ? "bg-dark" : "")" data-bs-theme="@ThemeName">
            <div class="container">
                <h1 class="navbar-brand" href="#">To-Do List</h1>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarColor02">
                    <ul class="navbar-nav me-auto">
                        @* <li class="nav-item">
                        <a class="nav-link active" href="#">
                        Home
                        <span class="visually-hidden">(current)</span>
                        </a>
                        </li>
                        <li class="nav-item">
                        <a class="nav-link" href="#">Features</a>
                        </li>
                        <li class="nav-item">
                        <a class="nav-link" href="#">Pricing</a>
                        </li>
                        <li class="nav-item">
                        <a class="nav-link" href="#">About</a>
                        </li>
                        <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
                        <div class="dropdown-menu">
                        <a class="dropdown-item" href="#">Action</a>
                        <a class="dropdown-item" href="#">Another action</a>
                        <a class="dropdown-item" href="#">Something else here</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Separated link</a>
                        </div>
                        </li> *@
                    </ul>
                    <div class="d-flex">
                        <button type="button" data-bs-toggle="tooltip" data-bs-original-title="Switch to @(ThemeName == _darkThemeName ? "Light" : "Dark") theme!" data-bs-placement="bottom" class="btn me-2" onclick="@(() => ToggleTheme())">
                            <span class="material-symbols-outlined">
                                @ThemeIcon
                            </span>
                        </button>
                        @* <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button> *@
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <div class="body-content min-vh-100">
        <div class="container">@Body</div>
    </div>
</main>

